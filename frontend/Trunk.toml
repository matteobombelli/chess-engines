# Local development proxy.
#
# The app calls the bot at its deployed path (`/projects/chessengines/api/…`) so
# the same build works behind the production reverse proxy. `trunk serve` does
# not know about that path, so it forwards it to the crate you run in the other
# terminal:
#
#   cargo run -p random   # 127.0.0.1:3000
#
# `public_url` matches the deployed prefix so the piece images, which the app
# addresses by absolute path, resolve under `trunk serve` as well.

[build]
public_url = "/projects/chessengines/"

[[proxy]]
rewrite = "/projects/chessengines/api/random/"
backend = "http://127.0.0.1:3000/"

[[proxy]]
rewrite = "/projects/chessengines/api/minimax/"
backend = "http://127.0.0.1:3002/"

[[proxy]]
rewrite = "/projects/chessengines/api/alphamini/"
backend = "http://127.0.0.1:3006/"

[[proxy]]
rewrite = "/projects/chessengines/api/minigpt/"
backend = "http://127.0.0.1:3008/"
