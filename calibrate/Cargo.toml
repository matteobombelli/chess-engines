[package]
name = "calibrate"
version = "0.1.0"
edition = "2024"

[dependencies]
arena = { path = "../arena" }
chess-core = { path = "../chess-core" }
minimax = { path = "../minimax" }
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
